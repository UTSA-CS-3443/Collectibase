package model;

import java.util.*;

public class CustomCollection extends Collections{
	
	Map<String,String> customMap;
	
	public CustomCollection() {
		customMap = new HashMap<String,String>();
	}
	/****
	 * Assuming that the custom collection is gonna be written with a mapping.
	 * The user will input the key, which is the variable name, and the value will
	 * also be inputed by the user. Ex: (key, value) = (title, "Pikachu") or whatever.
	 * Accessing the keys and values will be the same as in Lab 9. 
	 */
	
}
